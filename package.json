{
  "name": "example",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm --filter \"./apps/host-app\" run dev",
    "preview": "pnpm --filter \"./apps/host-app\" run preview",
    "build": "pnpm --filter \"./apps/host-app\" run build",
    "demo": "pnpm build && pnpm preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
    "rimraf": "^5.0.7"
  },
  "engines": {
    "node": "18.x"
  }
}
